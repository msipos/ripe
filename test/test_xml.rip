parse(node, i)
  for j in 0:i
    Out.print(" ")
  Out.println(node.name)
  if node.child != nil
    parse(node.child, i + 1)
  if node.next != nil
    parse(node.next, i)

main()
  xml_doc = "<?xml version=\"1.0\" encoding=\"ISO-8859-1\"?><!-- Edited by XMLSpy® --><note>	<to>Tove</to>	<from>Jani</from>	<heading>Reminder</heading>	<body>Don't forget me this weekend!</body></note>"

  doc = Xml.Document.new_from_text(xml_doc)
  root = doc.get_root()
  parse(root, 0)
